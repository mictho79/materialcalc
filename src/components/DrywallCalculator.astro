---
// Drywall Calculator — wall + ceiling area to sheet count, mud, tape, screws.
---

<div class="tool" id="drywall-tool">
  <div class="tool-header">
    <div class="tool-title">Calculator</div>
    <div class="unit-toggle" role="tablist" aria-label="Units">
      <button type="button" class="active" data-units="imperial" aria-pressed="true">Imperial</button>
      <button type="button" data-units="metric" aria-pressed="false">Metric</button>
    </div>
  </div>

  <div class="form-grid">
    <div class="field">
      <label for="dw-length">Room length</label>
      <div class="field-input">
        <input id="dw-length" type="number" value="14" min="0" step="0.5" inputmode="decimal" />
        <span class="suffix" data-unit="length">ft</span>
      </div>
    </div>
    <div class="field">
      <label for="dw-width">Room width</label>
      <div class="field-input">
        <input id="dw-width" type="number" value="12" min="0" step="0.5" inputmode="decimal" />
        <span class="suffix" data-unit="length">ft</span>
      </div>
    </div>
    <div class="field">
      <label for="dw-height">Ceiling height</label>
      <div class="field-input">
        <input id="dw-height" type="number" value="8" min="0" step="0.5" inputmode="decimal" />
        <span class="suffix" data-unit="length">ft</span>
      </div>
    </div>
    <div class="field">
      <label for="dw-sheet">Sheet size</label>
      <div class="field-input">
        <select id="dw-sheet">
          <option value="32" selected>4x8 ft (32 sq ft) — standard</option>
          <option value="48">4x12 ft (48 sq ft) — long walls</option>
          <option value="40">4x10 ft (40 sq ft) — tall walls</option>
          <option value="48-54">54-inch wide x 12 ft (54 sq ft) — 9-ft walls</option>
        </select>
      </div>
    </div>
    <div class="field">
      <label for="dw-doors">Number of doors</label>
      <div class="field-input">
        <input id="dw-doors" type="number" value="1" min="0" step="1" inputmode="decimal" />
        <span class="suffix">≈ 20 sq ft each</span>
      </div>
    </div>
    <div class="field">
      <label for="dw-windows">Number of windows</label>
      <div class="field-input">
        <input id="dw-windows" type="number" value="2" min="0" step="1" inputmode="decimal" />
        <span class="suffix">≈ 15 sq ft each</span>
      </div>
    </div>
    <div class="field">
      <label for="dw-ceiling">Include ceiling?</label>
      <div class="field-input">
        <select id="dw-ceiling">
          <option value="1" selected>Yes — include ceiling</option>
          <option value="0">No — walls only</option>
        </select>
      </div>
    </div>
    <div class="field">
      <label for="dw-price">Price per sheet <span class="optional">(optional)</span></label>
      <div class="field-input">
        <input id="dw-price" type="number" value="15" min="0" step="0.5" inputmode="decimal" />
        <span class="suffix">$/sheet</span>
      </div>
    </div>
  </div>

  <div class="result" role="status" aria-live="polite">
    <div class="result-label">You need</div>
    <div class="result-main">
      <span class="number" id="dw-sheets">19</span>
      <span class="unit">sheets</span>
      <span class="aux">for <strong id="dw-area">584</strong> sq ft</span>
    </div>
    <div class="result-secondary">
      Screws: ~<strong id="dw-screws">350</strong> &nbsp;·&nbsp;
      Joint compound: <strong id="dw-mud">2</strong> buckets (4.5 gal) &nbsp;·&nbsp;
      Tape: <strong id="dw-tape">365</strong> ft
    </div>
    <div class="result-cost-row">
      Estimated sheet cost: <strong id="dw-cost">$285</strong>
      <span class="note">plus mud, tape, screws, primer</span>
    </div>
    <div class="result-actions">
      <button type="button" class="btn" id="dw-btn-copy">📋 Copy result</button>
      <button type="button" class="btn" onclick="window.print()">🖨 Print</button>
      <button type="button" class="btn" id="dw-btn-share">↗ Share</button>
    </div>
  </div>
</div>

<style>
  .tool { background: var(--white); border: 1px solid var(--n200); border-radius: var(--r-lg); padding: var(--s-6); margin-bottom: var(--s-4); box-shadow: var(--shadow-xs); }
  @media (max-width: 540px) { .tool { padding: var(--s-4); } }
  .tool-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: var(--s-5); padding-bottom: var(--s-4); border-bottom: 1px solid var(--n200); flex-wrap: wrap; gap: var(--s-2); }
  .tool-title { font-size: 15px; font-weight: 700; color: var(--n700); text-transform: uppercase; letter-spacing: 0.5px; }
  .unit-toggle { display: inline-flex; background: var(--n100); border-radius: var(--r-md); padding: 3px; }
  .unit-toggle button { border: none; background: transparent; padding: 6px 14px; font-size: 13px; font-weight: 600; color: var(--n500); cursor: pointer; border-radius: var(--r-sm); }
  .unit-toggle button.active { background: var(--white); color: var(--primary); box-shadow: 0 1px 2px rgba(0,0,0,0.1); }
  .form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: var(--s-4); margin-bottom: var(--s-4); }
  @media (max-width: 540px) { .form-grid { grid-template-columns: 1fr; } }
  .field label { display: block; font-size: 13px; font-weight: 600; color: var(--n700); margin-bottom: 6px; }
  .optional { color: var(--n400); font-weight: 400; }
  .field-input { position: relative; display: flex; align-items: center; }
  .field input, .field select { width: 100%; height: 44px; padding: 0 14px; border: 1.5px solid var(--n200); border-radius: var(--r-md); font-size: 15px; color: var(--n900); background: var(--white); }
  .field input:focus, .field select:focus { outline: none; border-color: var(--primary); box-shadow: 0 0 0 3px rgba(30,58,95,0.1); }
  .suffix { position: absolute; right: 12px; color: var(--n500); font-size: 13px; font-weight: 500; pointer-events: none; }
  .result { background: var(--n50); border: 1px solid var(--n200); border-radius: var(--r-md); padding: var(--s-5); margin-top: var(--s-5); }
  .result-label { font-size: 12px; font-weight: 700; color: var(--n500); text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: var(--s-2); }
  .result-main { display: flex; align-items: baseline; gap: var(--s-2); flex-wrap: wrap; }
  .result-main .number { font-size: 36px; font-weight: 800; color: var(--accent); line-height: 1.1; letter-spacing: -1px; font-variant-numeric: tabular-nums; }
  .result-main .unit { font-size: 18px; color: var(--n700); font-weight: 600; }
  .result-main .aux { font-size: 14px; color: var(--n600); margin-left: 6px; }
  .result-main .aux strong { color: var(--n900); font-weight: 700; }
  .result-secondary { font-size: 15px; color: var(--n700); margin-top: var(--s-2); font-variant-numeric: tabular-nums; }
  .result-secondary strong { color: var(--n900); font-weight: 700; }
  .result-cost-row { margin-top: var(--s-2); padding-top: var(--s-3); border-top: 1px solid var(--n200); font-size: 14px; color: var(--n700); font-variant-numeric: tabular-nums; }
  .result-cost-row strong { color: var(--n900); font-weight: 700; }
  .note { color: var(--n500); font-weight: 400; font-size: 12px; margin-left: 8px; }
  .result-actions { display: flex; gap: var(--s-2); margin-top: var(--s-4); flex-wrap: wrap; }
</style>

<script is:inline>
(function() {
  const root = document.getElementById('drywall-tool');
  if (!root) return;
  const $ = (s) => root.querySelector(s);

  function calculate() {
    const units = root.querySelector('.unit-toggle button.active').dataset.units;
    let l = parseFloat($('#dw-length').value) || 0;
    let w = parseFloat($('#dw-width').value) || 0;
    let h = parseFloat($('#dw-height').value) || 0;
    const doors = parseInt($('#dw-doors').value) || 0;
    const windows = parseInt($('#dw-windows').value) || 0;
    const sheetSizeRaw = $('#dw-sheet').value;
    const sheetSize = parseFloat(sheetSizeRaw.split('-')[0]) || 32;
    const includeCeiling = parseInt($('#dw-ceiling').value);
    const sheetPrice = parseFloat($('#dw-price').value) || 0;

    if (units === 'metric') {
      l = l * 3.2808; w = w * 3.2808; h = h * 3.2808;
    }

    const perim = 2 * (l + w);
    let wallArea = perim * h;
    wallArea = Math.max(0, wallArea - (doors * 20) - (windows * 15));
    const ceilingArea = includeCeiling ? l * w : 0;
    const totalArea = wallArea + ceilingArea;
    const sheets = Math.ceil(totalArea / sheetSize * 1.10); // 10% waste
    const screws = Math.ceil(sheets * 18); // ~18 screws per sheet
    // Joint compound: ~150 sq ft per gallon for full job; 4.5 gal bucket
    const mudBuckets = Math.ceil(totalArea / 500);
    // Tape: ~38 linear ft per sheet of drywall installed
    const tapeFt = Math.ceil(sheets * 19);
    const cost = sheets * sheetPrice;

    $('#dw-sheets').textContent = sheets.toLocaleString();
    $('#dw-area').textContent = Math.round(totalArea).toLocaleString();
    $('#dw-screws').textContent = screws.toLocaleString();
    $('#dw-mud').textContent = mudBuckets.toString();
    $('#dw-tape').textContent = tapeFt.toLocaleString();
    $('#dw-cost').textContent = '$' + Math.round(cost).toLocaleString();
  }

  root.querySelectorAll('input, select').forEach(el => el.addEventListener('input', calculate));

  root.querySelectorAll('.unit-toggle button').forEach(btn => {
    btn.addEventListener('click', () => {
      root.querySelectorAll('.unit-toggle button').forEach(b => {
        b.classList.remove('active');
        b.setAttribute('aria-pressed', 'false');
      });
      btn.classList.add('active');
      btn.setAttribute('aria-pressed', 'true');
      const metric = btn.dataset.units === 'metric';
      root.querySelectorAll('[data-unit="length"]').forEach(el => el.textContent = metric ? 'm' : 'ft');
      calculate();
    });
  });

  $('#dw-btn-copy').addEventListener('click', () => {
    const text = `Drywall needed: ${$('#dw-sheets').textContent} sheets for ${$('#dw-area').textContent} sq ft. Cost: ${$('#dw-cost').textContent}.`;
    navigator.clipboard?.writeText(text).then(() => {
      const btn = $('#dw-btn-copy');
      const o = btn.textContent;
      btn.textContent = '✓ Copied';
      setTimeout(() => { btn.textContent = o; }, 1500);
    });
  });

  $('#dw-btn-share').addEventListener('click', () => {
    if (navigator.share) navigator.share({ title: 'Drywall Calculator', url: location.href });
    else navigator.clipboard?.writeText(location.href);
  });

  calculate();
})();
</script>
